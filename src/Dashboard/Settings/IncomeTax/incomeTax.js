import React from 'react';
import { StyleSheet, Platform, Image, Text, View, ScrollView, Button } from 'react-native';
import { Icon, Footer, FooterTab } from 'native-base';

const IncomeTaxScreen = () => {
    navigationOptions = {
        drawerLabel: 'income tax',
    };
    return(
        <View>
            <Text>Income Tex</Text>
        </View>
    );
}

export default IncomeTaxScreen;